# Default model for all LLM calls unless specifically overridden
DEFAULT_MODEL=openai:gpt-4o

# OpenAI API Key (if using OpenAI)
OPENAI_API_KEY=your-openai-api-key

# Anthropic API Key (if using Anthropic)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional overrides for specific tool components (will fall back to DEFAULT_MODEL if unset)
# PREPROCESSOR_MODEL=openai:o3-mini
# VALIDATOR_MODEL=anthropic:sonnet-3.7
# POSTPROCESSOR_MODEL=ollama:llama3

# Paths for recipes, logs, and output
RECIPES_DIR=./recipes
OUTPUT_ROOT=./outputs
LOGS_DIR=./logs
